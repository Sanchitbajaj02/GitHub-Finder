import { Fragment } from "react";

const About = () => {
  return (
    <Fragment>
      <h1>About this App</h1>
      <h3>
        It searches for all the Github users using the text inputted by the user
        in the form. It returns all the users matching the string in a grid
        format.
      </h3>
    </Fragment>
  );
};

export default About;
